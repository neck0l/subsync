SUBSYNC FORK — BUILD LOGIC & PROGRESS LOG
=========================================
Plain-English running log so we never lose context between steps.
Newest entries at the bottom. Do not delete history; append only.

REPO LOCATION
-------------
Source repo (git):   C:\Users\ki\Desktop\subsync-master\subsync-master
Working install copy: C:\Users\ki\Desktop\subsync-master\subsync   (Python 3.6 frozen, reference only)
Native deps staged:  C:\subsync-deps
Installed assets:    C:\ProgramData\subsync\assets  (eng speech model + eng-hrv dict) - shared with our build

KNOWN-GOOD REFERENCE STACK (from the working install)
-----------------------------------------------------
Python 3.6.8 (frozen) | FFmpeg 5.1 (avcodec-59) | classic PocketSphinx+SphinxBase
wxPython 4.1 (wxWidgets 3.1.5) | MSVC 140 | pycryptodome

OUR BUILD STACK (reproduced on this machine)
--------------------------------------------
Python 3.11.9 | FFmpeg 5.1.2 (GyanD shared) | classic PocketSphinx (tag last-pre-1.0) +
SphinxBase (classic default branch) | MSVC 14.51 (VS "18" Community) | pybind11 3.0.4 | wxPython 4.2.5

=====================================================================
SEGMENT A - GIT BASELINE                                     [DONE]
=====================================================================
What was done:
- git init in the source repo (NOT the outer folder, so the binary install copy stays out of git).
- Set LOCAL git identity (repo-only): user.name="SubSync Fork", user.email="subsync-fork@localhost".
- Set core.autocrlf=false so the pristine Unix line endings are preserved.
- Commit 1 (64d6281): "baseline: pristine upstream sc0ty/subsync source (archived)" - source only.
- Commit 2 (d7d2fb9): "docs: add modernization roadmap" - adds MODERNIZATION_ROADMAP.md.
- Tag "v-baseline" on commit 1 (human marker).
- Tag "0.16" on commit 1 (NUMERIC tag; required so setup.py's git-describe versioning
  produces a valid PEP 440 version - "v-baseline" alone yielded invalid "1.gd7d2fb9").
- Branch "modernize" created and checked out. "master" kept pristine.

Current branches: master (pristine), modernize (active).
Rollback: git switch master; or git reset --hard v-baseline.

=====================================================================
SEGMENT B - REPRODUCE KNOWN-GOOD BUILD (no C++ changes)      [DONE]
=====================================================================
Goal: build the gizmo C++ extension from source on Python 3.11 against FFmpeg 5.1 +
classic PocketSphinx, matching the working binary, with ZERO source edits.

Dependencies acquired (all under C:\subsync-deps):
1) FFmpeg 5.1.2 shared dev libs
   - Downloaded GyanD/codexffmpeg release 5.1.2 "full_build-shared" (.zip, 55 MB),
     extracted with 7-Zip, normalized to C:\subsync-deps\ffmpeg-5.1\ (include\, lib\, bin\).
   - Confirmed avcodec-59.dll (== FFmpeg 5.1, the version that still has
     avcodec_decode_subtitle2, removed in FFmpeg 6.0).
2) Classic PocketSphinx + SphinxBase (headers only from source; libs from working DLLs)
   - Cloned github.com/cmusphinx/sphinxbase (classic headers still on default branch:
     include\sphinxbase\*.h + include\win32\config.h & sphinx_config.h).
   - Cloned github.com/cmusphinx/pocketsphinx and checked out tag "last-pre-1.0"
     (the classic ps_args/cmd_ln_* API; current master is the incompatible 5.x rewrite).
   - IMPORT LIBS: we did NOT rebuild the fragile old VS solutions. Instead we generated
     .lib import libraries directly from the KNOWN-GOOD working DLLs:
       * Copied sphinxbase.dll / pocketsphinx.dll from the install copy into
         <dep>\bin\Release\x64\ (the path setup.py expects on Windows).
       * dumpbin /exports  -> parsed symbol names -> wrote .def files
         (sphinxbase: 369 symbols, pocketsphinx: 105 symbols; verified ps_init,
          ps_process_raw, ps_args, cmd_ln_parse_r, err_set_callback all present).
       * lib /def /machine:x64 -> sphinxbase.lib + pocketsphinx.lib.
     This guarantees ABI match with the proven-working DLLs.
   Helper: C:\subsync-deps\run-msvc.cmd  (calls vcvars64.bat then runs a command).

Build:
- Installed pybind11 3.0.4, setuptools, wheel into Python 3.11.
- Environment for the build:
     FFMPEG_DIR       = C:\subsync-deps\ffmpeg-5.1
     SPHINXBASE_DIR   = C:\subsync-deps\sphinxbase
     POCKETSPHINX_DIR = C:\subsync-deps\pocketsphinx
- Command:  py -3.11 setup.py build_ext --inplace
- Result: compiled all 26 .cpp files with MSVC 14.51, linked
  gizmo.cp311-win_amd64.pyd (only benign warnings: av_init_packet deprecated,
  a few double->float/size_t->unsigned narrowings). NO errors.
- Staged runtime DLLs next to gizmo.pyd (repo root): all FFmpeg 5.1 bin DLLs +
  sphinxbase.dll + pocketsphinx.dll. (These are .gitignored.)

Validation:
- import gizmo  -> OK; exposes Correlator, Demux, SpeechRecognition, Translator,
  Extractor, Resampler, AudioDec, SubtitleDec, Dictionary, NgramSplitter, etc.
- Generated config.py + version.py via  py -3.11 setup.py build_py
  (version_short = 0.16.1).
- Installed runtime deps: pysubs2, pycryptodome, PyYAML, requests, certifi, wxPython 4.2.5.
- CLI:  py -3.11 run.py --help    -> full option help prints.
- CLI:  py -3.11 run.py --version -> "subsync version 0.16.1 on win32".
- GUI:  import subsync.gui.mainwin -> OK (MainWin class loads; wxPython 4.2.5).
- Assets: config.assetdir resolves to C:\ProgramData\subsync\assets and sees the
  installed 'dict' + 'speech' models (our from-source build reuses them directly).

STATE OF THE CODEBASE
---------------------
- NO source files modified. Only additive artifacts created:
  MODERNIZATION_ROADMAP.md, BUILD_LOGIC.txt, doc/BUILD_WINDOWS.md,
  tools/build-gizmo-windows.ps1, run-app.cmd.
- Generated/ignored (not committed): subsync/config.py, subsync/version.py,
  gizmo.cp311-win_amd64.pyd, *.dll in repo root, build/.
- The app is RUNNABLE from source on Python 3.11 (CLI verified; GUI loads).

HOW TO RUN THE APP NOW
----------------------
Option 1 (launcher):   double-click / run  run-app.cmd            (GUI)
                       run-app.cmd --cli --help                   (CLI)
Option 2 (manual):     cd C:\Users\ki\Desktop\subsync-master\subsync-master
                       py -3.11 run.py

HOW TO REBUILD gizmo FROM SCRATCH
---------------------------------
See doc/BUILD_WINDOWS.md, or run:  tools\build-gizmo-windows.ps1

NEXT (per roadmap, NOT yet started)
-----------------------------------
Segment C: setup.py distutils->setuptools + pyproject.toml.
Segment D: FFmpeg 6/7 subtitle+channel-layout migration (version-gated).
Segment E: C++17 (throw()->noexcept).
Segments F/G/H/I: multi-engine speech (Vosk default, Whisper opt-in, Sphinx fallback).

=====================================================================
SEGMENT C - BUILD SYSTEM MODERNIZATION                       [DONE]
=====================================================================
- setup.py: removed 'import distutils...'; now uses setuptools.Command,
  setuptools.command.build_py.build_py, setuptools.errors.CompileError
  (guarded fallback to distutils for very old setuptools). gen_gui/gen_locales/
  gen_version/gen_doc rebased on setuptools.Command.
- Added pyproject.toml (PEP 517: setuptools>=64, wheel, pybind11>=2.10).
- python_requires >=3.5 -> >=3.9.
- Verified: rebuild OK, import gizmo OK, run --version = 0.16.1.

=====================================================================
SEGMENT E - C++17 MODERNIZATION                              [DONE]
=====================================================================
- gizmo/general/exception.{h,cpp}: all throw() -> noexcept.
- setup.py setup_unix: prefer -std=c++17 (fallback c++14/c++11).
- setup.py setup_msvc: add /EHsc /std:c++17 AND actually assign
  ext.extra_compile_args/extra_link_args (previously MSVC flags were computed
  but never applied - only the compiler default was used).
- Verified: rebuild shows /std:c++17 on the compile line, 0 errors, import OK.

=====================================================================
SEGMENT L - DEPENDENCY UPDATES                               [DONE]
=====================================================================
- setup.py install_requires + requirements.txt floors raised:
  pysubs2>=1.6, PyYAML>=6.0, requests>=2.31, pybind11>=2.10,
  pycryptodome>=3.19, cryptography>=42.0 (Darwin), wxPython>=4.2.
- Confirmed yaml.safe_load already used in synchro/task.py (no unsafe load).
- Verified against installed versions; imports + run OK.

=====================================================================
SEGMENT D - FFMPEG 6/7 COMPATIBILITY                         [DONE]
=====================================================================
- Reconnaissance against FFmpeg 7.1 headers (BtbN, avcodec-61):
    * STILL PRESENT: avcodec_decode_subtitle2, av_init_packet (no rewrite needed).
    * REMOVED: av_get_default_channel_layout, av_get_channel_name,
      av_get_channel_description, av_get_channel_layout_channel_index,
      swr_alloc_set_opts, and AVFrame/AVCodecContext channels/channel_layout fields.
    * The replacement AVChannelLayout / av_channel_layout_* / swr_alloc_set_opts2
      API exists in BOTH 5.1 and 7.1.
- Added gizmo/media/ffmpeg-channel-compat.h: inline helpers that keep the internal
  uint64_t channel-layout bitmask but talk to the new struct API at the FFmpeg
  boundary (su_get_channels, su_get_channel_layout, su_get_default_channel_layout,
  su_set_frame_layout, su_ensure_frame_layout, su_get_channel_name/description,
  su_get_channel_index, su_swr_set_opts). Python AudioFormat API unchanged.
- Migrated stream.cpp, audiodec.cpp, resampler.cpp to the helpers.
- Downloaded FFmpeg 7.1 dev libs to C:\subsync-deps\ffmpeg-7.1 (import libs present).
- Verified: 0-error build + import on FFmpeg 5.1 AND FFmpeg 7.1. Restored the
  known-good FFmpeg 5.1 build + DLLs as the active runtime.

NEXT (per roadmap, NOT yet started)
-----------------------------------
Segments F/G/H/I: multi-engine speech (Vosk default, Whisper opt-in, Sphinx fallback).
Segment J: sync-precision improvements. Segment K: asset hosting for new models.
Segment M: GUI engine selector. Segment N: installers (full + portable). Segment O: tests.
